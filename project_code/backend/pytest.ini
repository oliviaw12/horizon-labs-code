[pytest]
asyncio_mode = auto
testpaths = tests
addopts = --cov=app --cov=clients --cov-branch --cov-report=term-missing --cov-report=xml --cov-fail-under=70
markers =
    integration: marks a test as requiring FastAPI integration layers